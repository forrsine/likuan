# 2026-07-02 开发记录

## 对应计划

系统设计文档第 9 章“解析器（第 3-4 天）”：完成 lexer、parser、AST dump 和语法错误定位。

## 今日完成

- 将字符集操作拆分为 `src/charset.c`。
- 实现独立词法分析器 `src/lexer.c`，token 记录原始模式中的字节位置。
- 实现递归下降解析器 `src/parser.c`，优先级为重复、连接、选择。
- AST 保留 `{m,n}` 参数和捕获组编号，为后续捕获组回填做准备。
- 新增 `rx_dump_ast`，可输出树形 AST。
- 新增 lexer、parser 单元测试，并保留 API 回归测试。
- 错误输入可返回分类错误码和字节位置，例如 `a**` 定位到 byte 2。

## 验证结果

```text
lexer tests passed
parser tests passed
all tests passed
```

示例：

```powershell
rx_dump_ast "([a-z]+)\d{2,4}"
```

输出包含 `GROUP #1`、`PLUS`、`REPEAT {2,4}` 和对应字符类节点。

## 下一步

拆分 NFA 与 matcher 模块，增加 NFA 状态转移表输出，为子集构造 DFA 准备稳定输入。
