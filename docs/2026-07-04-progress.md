# 2026-07-04 开发记录

## 对应计划

系统设计文档第 9 章“第 5 天 NFA”：完成 Thompson 构造、NFA 状态转移表和 NFA 模式匹配。

## 今日完成

- 清理仓库中遗留的 Git 冲突标记，恢复可编译基线。
- 完成 `nfa` 与 `matcher` 模块拆分，公开 API 只负责资源管理和调用编排。
- 统一字符集文本输出，Token、AST 和 NFA 使用相同的字符类格式。
- 新增 NFA 状态数、转移数和转移类型统计。
- 新增 `rx_dump_nfa`，打印起始状态、接受状态和逐状态转移表。
- 新增 `test_nfa`，验证 literal、alternate、star 的 Thompson 结构。
- 验证 NFA 锚点语义和同一起点最长匹配。

## Thompson 构造验证

```text
pattern  states  transitions  start  accept
a        2       1            0      1
a|b      6       6            4      5
a*       4       5            2      3
```

演示命令：

```powershell
.\build-manual\rx_dump_nfa.exe 'a|b'
.\build-manual\rx_dump_nfa.exe 'a*'
```

## 验证结果

```text
lexer tests passed
parser tests passed
NFA tests passed
all tests passed
```

## 下一步

进入第 6～7 天 DFA 阶段：实现 NFA 状态集合表示、epsilon-closure 子集构造和 DFA 执行器。
