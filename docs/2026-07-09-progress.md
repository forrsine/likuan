# 2026-07-09 开发记录

## 对应计划

第 10 天：建立 NFA/DFA 正确性对照和性能基准，增加压力测试与内存检查入口。

## 上一步复查

重新扫描 Word 需求、设计文档、Git 历史和全部源码后，发现第 9 天提交中存在已入库的 Git 冲突标记与两套重复捕获实现。先完成基线修复，再开展第 10 天工作。

## 今日完成

- 清除 README 和核心源码中的全部冲突标记。
- 将整体 NFA 匹配与捕获回填拆为 `matcher.c` 和 `capture.c`。
- 移除固定 32 组捕获上限，压力测试验证 64 个连续捕获组。
- DFA 子集状态改用位图哈希桶去重。
- 新增 `regex_get_stats`，返回 NFA、子集 DFA、MinDFA 和字符等价类统计。
- 新增 `test_stress`，覆盖 4096 字节 nested-plus 场景、失败尾字符、编译释放循环和多捕获组。
- 新增 Linux 条件测试 `test_posix`，对公共 ERE 子集比较 NFA、DFA 与 POSIX `regex.h`。
- 新增 `rx_bench`，统计编译耗时、匹配耗时和状态数量。
- CMake 新增 `RX_ENABLE_SANITIZERS`，支持 ASan/UBSan。
- README 补充 benchmark、POSIX、sanitizer 和 Valgrind 命令。

## 验证结果

Windows Scope MSVC `/W4` 全量编译：

```text
lexer tests passed
parser tests passed
NFA tests passed
DFA tests passed
all tests passed
stress tests passed
```

全部工具编译无警告。Release 基准中的四个内置场景均显示 DFA 匹配快于 NFA；DFA 编译时间高于 NFA，符合预期。

## 环境限制

当前 Windows 环境没有 CMake、GCC、WSL 发行版、Valgrind 和 POSIX `regex.h`，因此 `test_posix`、POSIX 性能比例、ASan/UBSan、Valgrind 需要在 WSL/Linux 环境补跑。

## 下一步

第 11 天继续扩大公共语法用例集，在 WSL/Linux Release 环境采集 POSIX 对比数据，并生成报告用性能表。
