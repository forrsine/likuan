# 2026-07-10 开发记录

## 对应计划

第 11 天：扩大 NFA/DFA/POSIX 正确性对照，稳定性能基准，并生成报告可直接使用的数据表。

## 今日完成

- 新增 `test_conformance`，覆盖全部基础语法的正例、反例、match 和 search。
- 45 个符合性组合同时执行 NFA 与 DFA，共 90 次执行全部通过。
- POSIX 条件测试扩展为 20 个公共 ERE 模式、17 个文本、match/search 和 NFA/DFA 双模式。
- POSIX 测试完成时输出通过数量与通过率。
- `rx_bench` 新增 `--csv FILE`，保存编译耗时、匹配耗时、状态数量和相对 NFA 加速比。
- benchmark 新增 CTest smoke test，低迭代计时不足时给出明确提示。
- 使用 `/O2` 和 100000 次迭代采集 Windows NFA/DFA 数据。
- 生成 `performance-day11.csv` 和 `day11-performance-report.md`。

## 回归结果

```text
lexer tests passed
parser tests passed
NFA tests passed
DFA tests passed
all tests passed
stress tests passed
conformance tests passed (90 executions)
```

所有目标使用 MSVC `/W4` 编译，无警告。

## 性能结论

四个内置场景中，DFA 匹配速度约为 NFA 的 14.5 至 28.2 倍；DFA 编译时间更高。对于模式复用次数多的场景，DFA 更有优势。

## 未完成的外部验证

本机仍无 WSL 发行版、GCC、CMake、Valgrind 和 POSIX `regex.h`，因此 POSIX 通过率、DFA/POSIX 80% 目标与内存工具报告需要在 WSL/Linux 中运行。

## 下一步

进入第 12 天：实现 NFA/DFA DOT 导出，生成 Graphviz 图并补入报告与答辩材料。
